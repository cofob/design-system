import manifest from "../manifests/flunky-all/047.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll047Sticker = manifest as StaticStickerAsset;
