import manifest from "../manifests/flunky-all/019.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll019Sticker = manifest as StaticStickerAsset;
