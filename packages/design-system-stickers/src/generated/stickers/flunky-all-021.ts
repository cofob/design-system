import manifest from "../manifests/flunky-all/021.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll021Sticker = manifest as StaticStickerAsset;
