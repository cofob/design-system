import manifest from "../manifests/ph-silver/100.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver100Sticker = manifest as StaticStickerAsset;
