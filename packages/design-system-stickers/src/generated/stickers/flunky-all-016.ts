import manifest from "../manifests/flunky-all/016.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll016Sticker = manifest as StaticStickerAsset;
