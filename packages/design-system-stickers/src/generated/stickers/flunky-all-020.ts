import manifest from "../manifests/flunky-all/020.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll020Sticker = manifest as StaticStickerAsset;
