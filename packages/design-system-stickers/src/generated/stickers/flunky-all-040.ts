import manifest from "../manifests/flunky-all/040.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll040Sticker = manifest as StaticStickerAsset;
