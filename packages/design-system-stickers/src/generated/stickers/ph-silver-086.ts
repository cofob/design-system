import manifest from "../manifests/ph-silver/086.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver086Sticker = manifest as StaticStickerAsset;
