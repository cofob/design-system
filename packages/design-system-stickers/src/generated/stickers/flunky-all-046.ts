import manifest from "../manifests/flunky-all/046.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll046Sticker = manifest as StaticStickerAsset;
