import manifest from "../manifests/ph-silver/092.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver092Sticker = manifest as StaticStickerAsset;
