import manifest from "../manifests/flunky-all/018.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll018Sticker = manifest as StaticStickerAsset;
