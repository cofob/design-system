import manifest from "../manifests/flunky-all/003.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll003Sticker = manifest as StaticStickerAsset;
