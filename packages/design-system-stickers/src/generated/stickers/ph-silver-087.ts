import manifest from "../manifests/ph-silver/087.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver087Sticker = manifest as StaticStickerAsset;
