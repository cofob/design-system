import manifest from "../manifests/ph-silver/071.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver071Sticker = manifest as StaticStickerAsset;
