import manifest from "../manifests/ph-silver/023.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver023Sticker = manifest as StaticStickerAsset;
