import manifest from "../manifests/ph-silver/026.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const phSilver026Sticker = manifest as StaticStickerAsset;
