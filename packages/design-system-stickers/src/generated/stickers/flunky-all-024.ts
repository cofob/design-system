import manifest from "../manifests/flunky-all/024.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll024Sticker = manifest as StaticStickerAsset;
