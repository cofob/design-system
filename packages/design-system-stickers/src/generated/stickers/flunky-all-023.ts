import manifest from "../manifests/flunky-all/023.json" with { type: "json" };
import type { StaticStickerAsset } from "../../types.js";

export const flunkyAll023Sticker = manifest as StaticStickerAsset;
