---
interface Props {
  eyebrow: string;
  title: string;
  description: string;
}

const { eyebrow, title, description } = Astro.props;
---

<header class="astro-hero">
  <p class="astro-eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  <p>{description}</p>
</header>
